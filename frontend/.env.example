# Server-side proxy target (Next.js rewrites /api/* to this URL)
BACKEND_URL=http://127.0.0.1:8000

# Optional: set only if the browser must call the API directly (skip proxy)
# NEXT_PUBLIC_API_URL=http://localhost:8000
